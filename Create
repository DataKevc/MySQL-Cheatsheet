CREATE TABLE users(
id INT AUTO_INCREMENT,
first_name VARCHAR(50),
last_name VARCHAR(50),
email VARCHAR(50),
password VARCHAR(20),
is_admin TINYINT(1),
create_date DATETIME,
PRIMARY KEY(id)
);

> Example on creating a table called 'users', which primary key is 'id' and followed by several properties of the user
